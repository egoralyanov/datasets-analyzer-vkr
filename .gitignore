# --- Локальная база знаний и заметки разработчика (не публикуем) ---
.project_docs/
DEV_NOTES.md
HELLO.md
# Старые имена — на случай если что-то всплывёт.
.knowledge/
CLAUDE.md

# --- Планы разработки (локально, не публикуем) ---
plans/

# --- Материалы к защите (локально, не публикуем) ---
defense/

# --- Локальные данные пользователя (корень репо) ---
/data/

# --- ML-артефакты обучения мета-классификатора ---
# JSON-источники (real_set.json, synthetic_set.json) и meta_classifier_report.json коммитятся.
# Сами модели (.pkl) привязаны к версии sklearn — переобучаются через `make seed-all`.
backend/ml/models/*.pkl

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Виртуальные окружения
.venv/
venv/
env/
ENV/

# Pytest / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# mypy / ruff / type checkers
.mypy_cache/
.ruff_cache/
.pytype/

# --- Node / Frontend ---
node_modules/
dist/
.vite/
.cache/
*.local

# Логи
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Секреты / окружение ---
.env
.env.*
!.env.example

# --- IDE / OS ---
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# --- Docker ---
docker-compose.override.yml

# --- Локальный тулинг ---
.local_tools/
# Старое имя — на случай если что-то всплывёт.
.claude/

# --- Прочее ---
*.log
tmp/
temp/
